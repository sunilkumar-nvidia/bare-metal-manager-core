apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    app: keycloak
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8080
      name: http
  selector:
    app: keycloak
