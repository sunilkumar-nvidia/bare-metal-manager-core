## =============================================================================
## Vault-backed ClusterIssuer for Forge PKI
## Issues certificates via Vault PKI engine (forgeca/sign/forge-cluster).
## Requires: Vault PKI configured (vault-pki-config Job), cert-manager Vault SA.
## =============================================================================
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-forge-issuer
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "15"
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  vault:
    path: {{ .Values.vault.pkiMount }}/sign/{{ .Values.vault.pkiRole }}
    server: {{ .Values.vault.address }}
    caBundleSecretRef:
      name: site-root
      key: ca.crt
    auth:
      kubernetes:
        role: cert-manager-forge-issuer
        mountPath: /v1/auth/kubernetes
        secretRef:
          name: vault-forgeca-issuer-token
          key: token
