## Zalando postgres-operator 1.10.1
## Reference: carbide-external/manifests/forge-pg-operator-app/values.yaml

configTarget: ConfigMap

configKubernetes:
  ## Inject forge-pg-cluster-env ConfigMap into every postgres pod as env vars.
  ## The ConfigMap is created by carbide-prereqs (templates/postgresql.yaml)
  ## and contains site-specific values (TMP_SITE, etc.).
  pod_environment_configmap: "postgres/forge-pg-cluster-env"
